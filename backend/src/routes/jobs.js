const express = require('express');
const router = express.Router();

// Routes will be implemented in feature/backend-routes
// Stub to keep the server bootable during setup

module.exports = router;
